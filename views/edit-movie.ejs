<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <style>
        body {
            background-color: #1e1e1e;
            color: white;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }

        h1 {
            color: #e50914;
            text-align: center;
            margin-top: 40px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 300px;
            margin: auto;
            padding: 20px;
            border-radius: 10px;
            background-color: #141414;
        }

        label {
            color: white;
            display: block;
            margin-bottom: 8px;
        }

        input, textarea {
            width: calc(100% - 16px);
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #333;
            border-radius: 5px;
            background-color: #1e1e1e;
            color: white;
            box-sizing: border-box;
        }

        .button-container {
            display: flex;
            justify-content: space-between;
            width: 100%;
        }

        button, a {
            width: 60%;
            padding: 10px;
            background-color: #e50914;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block;
            transition: background-color 0.3s;
        }

        button:hover, a:hover {
            background-color: #ff0a16;
        }
        footer {
            width: 100%;
            margin-top: 42px;
            text-align: center;
            color: #8c8c8c;
        }

        .go-back {
            width: 50%;
            padding: 10px;
            background-color: #111;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .go-back:hover {
            background-color: #333;
        }
    </style>
</head>

<body>

    <h1>Edit Movie</h1>

    <form action="/admin/edit-movie/<%= movie._id %>" method="post">
        <div>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= movie.title %>" required>
        </div>
        <div>
            <label for="description">Description:</label>
            <textarea id="description" name="description" rows="4" required><%= movie.description %></textarea>
        </div>
        <div>
            <label for="image1">Image 1 URL:</label>
            <input type="text" id="image1" name="image1" value="<%= movie.image1 %>" required>
        </div>
        <div>
            <label for="image2">Image 2 URL:</label>
            <input type="text" id="image2" name="image2" value="<%= movie.image2 %>" required>
        </div>
        <div>
            <label for="image3">Image 3 URL:</label>
            <input type="text" id="image3" name="image3" value="<%= movie.image3 %>" required>
        </div>
        <div>
            <label for="trailer">Trailer URL:</label>
            <input type="text" id="trailer" name="trailer" required>
        </div>
        <div class="button-container">
            <button type="submit">Save Changes</button>
            <button class="go-back" onclick="window.history.back()">Go Back</button>
        </div>
    </form>
    <footer>
        Adil Daniyar SE-2206
    </footer>

</body>

</html>
